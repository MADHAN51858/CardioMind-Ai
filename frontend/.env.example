# CardioMind Frontend — Environment Template
# Copy this file to frontend/.env for local development.
# On Vercel, /api is same-origin — no VITE_API_BASE needed.

# Local dev: point to the FastAPI backend
VITE_API_BASE=http://localhost:8000/api
